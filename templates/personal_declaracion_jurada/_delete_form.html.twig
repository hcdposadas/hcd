<form method="post" action="{{ path('personal_declaracion_jurada_delete', {'id': personal_declaracion_jurada.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ personal_declaracion_jurada.id) }}">
    <button class="btn">Delete</button>
</form>
