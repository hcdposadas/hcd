{% extends 'base.html.twig' %}
{% block title %}
    Asignar Dictámenes al Orden del Día
{% endblock title %}
{% block body %}
    <div class="row">
        <div class="col-md-12">
            <div class="box">
                <div class="box-header">
                    <h3 class="box-title">Asignar Dictámenes al Orden del Día - {{ sesion.titulo }}</h3>
                    <div class="box-tools pull-right">

                    </div>
                </div>
                <div class="box-body">
                    {{ form_start(form) }}
                    {{ form_widget(form) }}
                    <button class="btn btn-primary"><i class="fa fa-save"></i> Guardar</button>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-md-12">
            <a class="btn btn-default" href="{{ path('sesiones_conformar_plan_de_labor_index') }}">Volver</a>
        </div>
    </div>
{% endblock body %}