{% extends 'base.html.twig' %}

{% block title %}Editar Novedad{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-6">
            <div class="card">
                <div class="card-header">
                    <h3 class="card-title"><i class="fas fa-newspaper"></i> Datos de la novedad</h3>
                </div>
                <div class="card-body">

                    {{ include('personal_novedad/_form.html.twig', {'button_label': 'Guardar'}) }}
                </div>
            </div>
        </div>
    </div>
    <a class="btn btn-default" href="{{ path('personal_novedades', {'persona': personal_novedad.legajo.persona.id}) }}">Volver</a>

{% endblock %}
