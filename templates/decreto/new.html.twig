{% extends 'base.html.twig' %}

{% block title %}
Decreto
{% endblock title %}
{% block body %}
    <div class="card">
    <div class="card-header">
        <h3 class="card-title">Decreto</h3>
    </div>
    <div class="card-body">

        {{ form_start(form) }}
            {{ form_widget(form) }}
            <input class="btn btn-primary" type="submit" value="Crear" />
        {{ form_end(form) }}
    </div>
    </div>


<a class="btn btn-default" href="{{ path('decreto_index') }}">Volver</a>

{% endblock %}
