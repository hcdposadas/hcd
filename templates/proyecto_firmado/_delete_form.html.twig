<form method="post" action="{{ path('proyecto_firmado_delete', {'id': proyecto_firmado.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ proyecto_firmado.id) }}">
    <button class="btn">Delete</button>
</form>
