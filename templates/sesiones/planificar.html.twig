{% extends 'base.html.twig' %}

{% block title %}
    Planificar Sesión
{% endblock title %}
{% block body %}
    <div class="card">
        <div class="card-header">
            <h3 class="card-title">{{ sesion }}</h3>
        </div>
        <div class="card-body">

            {{ form_start(form) }}
            {{ form_row(form.mociones) }}
            <input class="btn btn-primary" type="submit" value="Guardar"/>
            <div class="d-none">
                {{ form_end(form) }}
            </div>
        </div>
    </div>

    <a class="btn btn-default" href="{{ path('sesiones_index') }}">Volver</a>



{% endblock %}
