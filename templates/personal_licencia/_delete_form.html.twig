<form method="post" action="{{ path('personal_licencia_delete', {'id': personal_licencium.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ personal_licencium.id) }}">
    <button class="btn">Delete</button>
</form>
